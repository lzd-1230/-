#include<stdio.h>
	/*int sushu(int x);                  //素数 从100-200 
	int main(){ 
	int a,b,i;
	for(i=100;i<=200;i++){
	b=sushu(i);
	if(b==0) printf("%d不是素数",i);
	else printf("%d是素数",i);
	printf("\n");
}
	return 0;
 }  int sushu(int x){
	int t,j;
	for(j=2;j<=x-1;j++){
		t=x%j;
		if(t==0)
		break;
	}return (t);} */ 
	
	/*int main(){
		void change(int a,int b);
		char c[10];
		char d[10];
		int i; 
		for(i=0;i<=9;i++){                       //输入c组的字母等会复制给d 
			scanf("%s",&c[i]);
		}
		change(c[10],d[10]);                              //运行change函数 
		int j;
		for(j=0;j<=9;j++){                                  //输出数组d 
			printf("%s",d[j]);
		}
	return 0;
	}
	int change(char a[],char b[]){
		int i;
		for(i=0;i<10;i++){
			if(a[i]=='a'||a[i]=='e'||a[i]=='i'||a[i]=='o'||a[i]=='u')
			b[i]=a[i];
			return b[i];
			}
	*/                  wrong!
